export const appPermissions = Object.freeze({
  pacote: Object.freeze({
    ver: 'pacote:ver',
    editar: 'pacote:editar'
  }),
  remessa: Object.freeze({
    ver: 'remessa:ver',
    editar: 'remessa:editar'
  }),
  usuario: Object.freeze({
    ver: 'usuario:ver',
    editar: 'usuario:editar'
  })
})
